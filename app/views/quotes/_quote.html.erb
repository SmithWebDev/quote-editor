<%= turbo_frame_tag quote do %>
  <div class="quote">
    <%= link_to quote.name, quote_path(quote), data: { turbo_frame: '_top' } %>
    <div class="quote__actions">
      <!--constructing the turbo frames like this will have a side effect where,
      if another quote was being updated,
      and we deleted a different quote,
      the entire page would reload due to the _top link-->
      <%= button_to 'Delete', quote_path(quote), method: :delete, class: 'btn btn--light' %>
      <%= link_to 'Edit', edit_quote_path(quote), class: 'btn btn--light' %>
    </div>
  </div>
<% end %>
